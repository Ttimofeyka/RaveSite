• `npm run tw-watcher` : uses TailwindCSS to process the input.css file and creates an output.css file in the dist directory. It also watches for any changes made to the HTML and JS files and updates the output.css file accordingly.

• `npm run production` : runs the same process as tw-watcher but also minifies the output.css file and runs gulp tasks defined in this project.